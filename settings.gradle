pluginManagement {
    repositories {

        maven{
            url 'http://localhost:8081/repository/maven-public/'
            allowInsecureProtocol=true
            credentials{
                username 'sena'
                password ''
            }
        }

        //gradlePluginPortal()
        //google()
        //mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {

        maven{
            url 'http://localhost:8081/repository/maven-public/'
            allowInsecureProtocol=true
            credentials{
                username 'sena'
                password ''
            }
        }

        //google()
        //mavenCentral()
    }
}
rootProject.name = "StudyGuide"
include ':app'
